---
name: Update Formula (Dispatch)
# Triggered by package repos after PyPI publish
on:
  repository_dispatch:
    types: [update-formula]
# Deny-by-default; each job opts back into exactly what it needs.
permissions: {}
# Expected payload:
# {
#   "formula": "example",         # Formula name (required)
#   "version": "0.4.0"            # New version (optional, will check PyPI if not provided)
# }
jobs:
  update:
    # Builds the bumped formula from source now, not just audits it.
    timeout-minutes: 60
    name: Update formula (dispatch)
    # macOS runner: brew update-python-resources re-resolves the tree on the
    # host platform, so a macOS-only formula (depends_on :macos)
    # must be resolved on macOS or its macOS backends get swapped for Linux ones.
    # Matches update-formulas.yml and both cask workflows.
    runs-on: macos-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - name: Validate payload
        # The dispatch payload is attacker-controllable. Read it ONLY through env
        # (never interpolate ${{ ... }} into the script source, which would let a
        # malicious input execute), and validate against strict
        # patterns before any downstream use. Validated values are persisted to
        # $GITHUB_ENV so later steps consume the sanitized data, not the raw event.
        env:
          RAW_FORMULA: ${{ github.event.client_payload.formula }}
          RAW_VERSION: ${{ github.event.client_payload.version }}
        run: |
          if [ -z "$RAW_FORMULA" ]; then
            echo "::error::'formula' is required in client_payload"; exit 1
          fi
          # Homebrew formula token: lowercase alphanumerics plus . _ + - and @
          # (the last for versioned formulae like python@3.14).
          if ! printf '%s' "$RAW_FORMULA" | grep -qE '^[a-z0-9][a-z0-9._+@-]*$'; then
            echo "::error::invalid formula token (must match ^[a-z0-9][a-z0-9._+@-]*\$)"; exit 1
          fi
          # Version is optional; if present, restrict to safe version characters.
          if [ -n "$RAW_VERSION" ] && ! printf '%s' "$RAW_VERSION" | grep -qE '^[A-Za-z0-9][A-Za-z0-9._+-]*$'; then
            echo "::error::invalid version (must match ^[A-Za-z0-9][A-Za-z0-9._+-]*\$)"; exit 1
          fi
          if [ ! -f "Formula/${RAW_FORMULA}.rb" ]; then
            echo "::error::Formula '${RAW_FORMULA}' not found"; exit 1
          fi
          {
            echo "FORMULA=$RAW_FORMULA"
            echo "VERSION=$RAW_VERSION"
          } >> "$GITHUB_ENV"
          echo "Validated formula: $RAW_FORMULA"
      - name: Set up Homebrew
        uses: Homebrew/actions/setup-homebrew@c69e528904c52915e7f40e120fc7d49773bbb2a0  # main
      - name: Check for update
        id: check
        run: |
          repository="${GITHUB_REPOSITORY#*/}"
          tap_name="${GITHUB_REPOSITORY_OWNER}/${repository#homebrew-}"
          ref="${tap_name}/${FORMULA}"

          if [ -n "$VERSION" ]; then
            # Version provided in payload (already validated), use it directly.
            echo "version=$VERSION" >> "$GITHUB_OUTPUT"
            echo "has_update=true" >> "$GITHUB_OUTPUT"
            echo "Using provided version: $VERSION"
          else
            # Use livecheck to find the latest version
            livecheck_output=$(brew livecheck --json "$ref" 2>/dev/null || echo "[]")
            is_outdated=$(echo "$livecheck_output" | jq -r '.[0].version.outdated // false')

            if [ "$is_outdated" = "true" ]; then
              latest=$(echo "$livecheck_output" | jq -r '.[0].version.latest')
              echo "version=$latest" >> "$GITHUB_OUTPUT"
              echo "has_update=true" >> "$GITHUB_OUTPUT"
              echo "Found update: $latest"
            else
              echo "has_update=false" >> "$GITHUB_OUTPUT"
              echo "Formula is already up to date"
            fi
          fi
      - name: Update formula
        if: steps.check.outputs.has_update == 'true'
        env:
          RESOLVED_VERSION: ${{ steps.check.outputs.version }}
        run: |
          repository="${GITHUB_REPOSITORY#*/}"
          tap_name="${GITHUB_REPOSITORY_OWNER}/${repository#homebrew-}"
          ref="${tap_name}/${FORMULA}"

          echo "Updating $FORMULA to $RESOLVED_VERSION..."

          # Use brew bump-formula-pr to update the formula (without creating PR)
          brew bump-formula-pr \
            --no-audit \
            --no-browse \
            --write-only \
            --version="$RESOLVED_VERSION" \
            "$ref"

          # A virtualenv formula's resource blocks must be re-resolved: bump does
          # NOT touch them. Skip the command for a formula that has no resources,
          # but let a genuine failure fail the job — `brew audit` below checks
          # style and metadata, not the resolved dependency tree, so it cannot
          # catch stale pins and an unverified bump must not reach a PR.
          if grep -q "virtualenv_install_with_resources" "Formula/${FORMULA}.rb"; then
            # Recover the extras the formula was scaffolded with. Homebrew infers
            # the package name from the stable URL, which carries no extras, so a
            # bare re-resolve drops every extras-only resource.
            extras=$(sed -n 's/^[[:space:]]*# homebrew-tap:extras=//p' \
              "Formula/${FORMULA}.rb" | head -1)
            if [ -n "$extras" ]; then
              brew update-python-resources --package-name="${FORMULA}[${extras}]" "$ref"
            else
              brew update-python-resources "$ref"
            fi
          else
            echo "$FORMULA has no virtualenv resources; skipping update-python-resources"
          fi

          # The PR is opened with GITHUB_TOKEN and so won't trigger tests.yml.
          # Audit HERE instead so the auto-PR is verified inside the run that
          # creates it (mirrors update-cask-dispatch.yml). A failure fails the
          # job, so no unverified PR is opened.
          brew audit --strict --online "$ref"

          # `brew audit` is static: it never compiles a resource. Build and test
          # the bump so a dependency that fails to build cannot reach a PR
          # looking verified. A failure here fails the job, so no PR is opened.
          brew install --build-from-source "$ref"
          brew test "$ref"
          brew uninstall --force "$ref" || true
      - name: Create Pull Request
        if: steps.check.outputs.has_update == 'true'
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1  # v8.1.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore(${{ env.FORMULA }}): update to ${{ steps.check.outputs.version }}"
          title: "chore(${{ env.FORMULA }}): update to ${{ steps.check.outputs.version }}"
          body: |
            Automated update triggered by package release.

            - **Formula**: ${{ env.FORMULA }}
            - **Version**: ${{ steps.check.outputs.version }}

            Audited, built from source, and tested in the workflow run that
            opened this PR. Bottles are **not** built here: a PR opened with
            `GITHUB_TOKEN` does not trigger `tests.yml`. Close and reopen this PR
            (or push to its branch) to run the bottle build before applying the
            `pr-pull` label.

            ---
            *Triggered by repository_dispatch from package repo.*
          branch: auto-update/${{ env.FORMULA }}
          delete-branch: true
